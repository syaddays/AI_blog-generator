services:
  - type: web
    name: ai-blog-generator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn assessment:app
    envVars:
      - key: HUGGINGFACE_API_KEY
        sync: false
